write small codes for:

1. create a thread and write a fn to sum numbers till a given limit. This fn should be executed on this new thread. demonstrate the access of same address space
between this background thread and main fn thread. the fn takes input from pointers and returns 
ans through global variable.

2. write small code to demonstrate a deadlock scenario.

3. write program to create 2 threads and print even odd numbers.
